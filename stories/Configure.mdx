import { Meta } from '@storybook/blocks';

<Meta title='Slobbie RN UI Kit' />

<div className="sb-container">
  <div className='sb-section-title'>
    # 🍎 Slobbie RN UI Kit

    React Native 및 Expo 기반의 재사용 가능한 UI 컴포넌트 라이브러리입니다.
    Atomic Design 패턴을 따르며, TypeScript로 완전히 타입화되어 있습니다.

  </div>

  <div className="sb-section">
    <div className="sb-section-item">
      ### ✨ 핵심 철학
      - **Atomic Design**: Atoms, Molecules, Layout으로 구조화된 컴포넌트 계층
      - **Compound Component**: 직관적이고 유연한 컴포넌트 사용 패턴
      - **Theme System**: 디자인 토큰 기반의 일관된 스타일링
    </div>

    <div className="sb-section-item">
      ### 🏗 프로젝트 구조
      - `shared/ui/atoms`: 가장 기본적인 UI 요소 (Button, Typo 등)
      - `shared/ui/molecules`: 조합된 컴포넌트 (Avatar, Badge 등)
      - `shared/ui/layout`: 레이아웃 시스템 (Container, Grid 등)
      - `shared/utils/lib`: 테마 토큰 및 유틸리티
    </div>

  </div>
</div>

<style>
  {`
  .sb-container {
    margin-bottom: 48px;
  }
  .sb-section {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 20px;
    margin-top: 32px;
  }
  .sb-section-item {
    padding: 24px;
    border: 1px solid #e1e8ee;
    border-radius: 8px;
    background-color: #f8fafc;
  }
  .sb-section-title {
    margin-bottom: 32px;
  }

    .sb-addon-img {
      right: 0;
      left: 0;
      top: 130px;
      bottom: 0;
      overflow: hidden;
      height: auto;
      width: 124%;
    }

    .sb-addon-img img {
      width: 1200px;
      transform: rotate(-12deg);
      margin-left: 0;
      margin-top: 48px;
      margin-bottom: -40px;
      margin-left: -24px;
    }
  }
  `}
</style>
